/**
 * Dashboard UI lives in: `src/features/dashboard/Dashboard.jsx`
 * (App.jsx imports from there so charts/recharts stay next to the feature.)
 */
export { default } from "../../features/dashboard/Dashboard.jsx";
