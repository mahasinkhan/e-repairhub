/**
 * Admin panel routes are registered in App.jsx as direct children of <Routes> /
 * the protected layout <Route> (React Router v6+ requirement).
 *
 * When adding a page:
 * 1. Create src/pages/<area>/<Name>.jsx (default export).
 * 2. Add <Route path="..." element={<... />} /> next to the routes below in App.jsx.
 * 3. Add an entry in constants/nav.config.js (+ icon in components/layout/Sidebar.jsx if new icon).
 */

export {};
