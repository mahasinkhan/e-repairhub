VITE_API_URL=http://localhost:3000

# Optional: only when admin is served under /admin/ on the same host as other panels.
# Leave unset for Vercel (root domain). Vercel build: set VITE_API_URL to your Render API URL.
# VITE_BASE_PATH=/admin/
