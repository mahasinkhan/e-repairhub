# Backend API (Render). Vercel: set VITE_API_URL to your Render API URL.
VITE_API_URL=http://localhost:3000

# Optional: only when delivery is served under /delivery/ on the same host.
# VITE_BASE_PATH=/delivery/
